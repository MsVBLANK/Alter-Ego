services:
  alterego:
    image: ghcr.io/msvblank/alter-ego:latest
    build: .
    container_name: alterego
    env_file:
      - .env
    volumes:
      - data:/home/node/app/Configs
    restart: unless-stopped

volumes:
  data:
